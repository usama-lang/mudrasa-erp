/node_modules
/public/images/uploads
/public/hot
/public/uploads
/public/modules
/public/storage
/storage/*.key
/vendor
/bootstrap/cache/*.php
.env
.env.local
.env.production
.env.backup
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
.DS_Store
public/.DS_Store
public/images/.DS_Store
public/images/modules

# Exclude anything from /modules folder.
# Modules are independently developed and distributed.
# Each module manages its own:
# - vendor/ directory (module-specific dependencies)
# - composer.lock (locked dependencies)
# Use `php artisan module:install-deps` to install module dependencies.
/modules/*
/modules/*/vendor/
# Keep the modules directory structure
!/modules/.gitkeep

# User translations.
/resources/user-lang/*
!/resources/user-lang/.gitkeep

# Exclude release builds
/releases

# Exclude module-specific builds (e.g., build-crm, build-hrm)
/public/build-*
.composer

# Eclude zip/tar.gz release packages
*.zip
*.tar.gz
